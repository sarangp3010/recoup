{
  "name": "recoup",
  "private": true,
  "version": "0.0.0",
  "description": "Recoup — local-first health & recovery companion for iOS + Android (React Native)",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.8"
  }
}
